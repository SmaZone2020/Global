## 开发准则

- 每完成一次模块开发(修复或更新)，都要提交commit到git仓库，并且不得携带co-author信息，必须保证commit提交风格统一，表现为"(feat/fix等)xxxxxx"。
- 在编写代码的过程中，必须保证代码风格一致，只能使用驼峰命名法，并且必须保持API接口中与数据体(类)中的项大小写一致，禁止出现不一致的情况。
- 在编写后端类的过程中，类的子变量名首字母必须大写，使用标准字母命名法，若该类需要被序列化为JSON，则使用序列化命名工具使用驼峰命名法为该项命名。
- 在进行开发的过程中，无论是前端或后端，都严禁大代码文件存在，当该文件的逻辑复杂时，必须将其拆分为多个分散的代码文件，非特殊情况下，代码文件长度不得超过350行。
- 在进行复杂、中大型项目开发时，在规划过程中必须询问业务方面的实现细节，比如需要什么接口等，不得擅自定义。
